{
  "name": "borderless-trips-monorepo",
  "version": "1.0.0",
  "description": "Fullstack Premium Travel & Visa Services Portal",
  "scripts": {
    "build": "npm install --production=false --prefix client && npm install --prefix server && npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
